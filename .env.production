# 生产环境（npm run build）
# ---------------------------------------------------------------------------
# 【部署后只改这两行】把域名换成你在 Render 上的后端地址：
#   Render 免费实例形如 https://bbs-app-backend.onrender.com
#   （注意：末尾不要带 /，VITE_STATIC_BASE 只填域名，不带 /api）
# ---------------------------------------------------------------------------
VITE_API_BASE=https://bbs-app-backend-zuzm.onrender.com/api
VITE_STATIC_BASE=https://bbs-app-backend-zuzm.onrender.com

# 如果将来用 Vercel 的 rewrites 把 /api、/uploads 反代到后端（前后端同源），
# 可以把上面改成 VITE_API_BASE=/api、VITE_STATIC_BASE= （留空）

# 部署在子路径时改这里（例如 https://xxx.vercel.app/admin/ → 填 /admin/），
# 部署在域名根目录保持 / 即可
VITE_BASE_PATH=/
